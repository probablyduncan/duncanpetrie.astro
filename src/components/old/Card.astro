---
// import { getCollection } from 'astro:content';

const { imageSource, title, text, url } = Astro.props;

// const posts = (await getCollection('blog')).sort(
// 	(a, b) => a.data.pubDate.valueOf() - b.data.pubDate.valueOf()
// );
---

<section>
    <img src={imageSource} />
    <h1>{title}</h1>
    <p>{text}</p>
    <a href={url}>Go!</a>
</section>